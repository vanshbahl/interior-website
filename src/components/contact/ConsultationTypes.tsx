import { motion } from 'motion/react'

const options = [{ title: 'Residential', text: 'A home built around your rhythms, rituals, and sense of place.' }, { title: 'Commercial', text: 'A distinct environment for work, hospitality, retail, or culture.' }, { title: 'Luxury Renovation', text: 'A considered transformation that honours a building’s original character.' }]

export function ConsultationTypes({ selectedType, onSelect }: { selectedType: string; onSelect: (value: string) => void }) {
  return <section className="bg-canvas py-28 sm:py-36 lg:py-44"><div className="mx-auto max-w-[1440px] px-6 md:px-10 lg:px-14"><div className="max-w-2xl"><p className="text-[0.65rem] font-semibold tracking-[0.2em] text-accent uppercase">Choose a conversation</p><h2 className="mt-6 font-display text-5xl leading-[0.9] tracking-[-0.045em] sm:text-6xl">How can we begin?</h2></div><div className="mt-14 grid border-t border-line md:mt-20 md:grid-cols-3">{options.map((option, index) => <motion.button key={option.title} type="button" onClick={() => onSelect(option.title)} whileTap={{ scale: 0.99 }} className={`min-h-56 border-b border-line p-6 text-left transition-colors md:border-b-0 md:border-r md:last:border-r-0 md:p-8 ${selectedType === option.title ? 'bg-ink text-inverse' : 'hover:bg-surface'}`}><p className={`text-[0.62rem] font-semibold tracking-[0.16em] uppercase ${selectedType === option.title ? 'text-white/55' : 'text-accent'}`}>0{index + 1}</p><h3 className="mt-10 font-display text-3xl tracking-[-0.035em] sm:text-4xl">{option.title}</h3><p className={`mt-4 max-w-xs text-sm leading-6 ${selectedType === option.title ? 'text-white/70' : 'text-muted'}`}>{option.text}</p></motion.button>)}</div></div></section>
}
